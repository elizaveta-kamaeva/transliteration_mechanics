no need | вытаскивать слова из названий на русском, и если обрабатываемый бренд есть среди них, то не транслитить его
заменить пары "нграмма":"нграмма" на "нграмма":set(нграммы) для всех языков. отдельно для конкретного языкового модуля + пары для наивного
расширить словарь конкретных слов для английского языка
	два списка слов: для всеобще изместных транслитов (лореаль), для которых нет других вариантов; и для спорных, когда мы генерим еще и транслиты через нграммы
done | скорректировать транслитерацию аббревиатур и отдельно стоящих букв

добавить классовую структуру
	классовая структура для каждого слова
	классовая структура для каждого модуля? не факт
rejected | в Processor в return заменить список периодов времени на словарь с ключами
убрать периоды времени из Processor и SpecRepl вообще
	тотальное время считать в trans.py, где n = len(raw_lines)
	прогресс тоже считать в trans
сделать структуру Processor.py похожей на Пашину и убрать одну из них
	не писать пары в список
	хешировать слова?
	отдельные слова экстендить к списку с наивным и специальным транслитом
	в итоге Processor будет выдавать список для одного сырого слова
Getter разбить на два модуля
	1 - достает сырое слово
	2 - достает слово для обработки
	все выкинуть в trans
Separator: изменять existing words в Processor непосредственно из самого подмодуля
SpecRepl перекинуть в mechanics
